[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9 && pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build --filter @trendforge/web"]

[start]
cmd = "node apps/web/.next/standalone/server.js"
